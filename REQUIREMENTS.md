# タイピング練習アプリ 要件整理

## 1. 目的

HTML と JavaScript で、プリセットされた単語リストを使ってタイピング練習ができるブラウザアプリを実装する。

ユーザーはプリセット一覧から練習セットを選び、ランダムに出題される 10 問をタイピングする。ミスタイプがあった単語は結果画面で確認でき、ミスタイプ単語だけを対象に再挑戦できる。再挑戦はミスタイプ単語が 0 件になるまで繰り返せる。

## 2. 想定する画面

### 2.1 プリセット一覧画面

- 用意されているプリセットファイルの一覧を表示する。
- 各プリセットには、以下の情報を表示する。
  - プリセット名
  - 説明
  - 収録ワード数
- ユーザーは一覧から 1 つのプリセットを選択して、プレイ画面へ進む。

### 2.2 プレイ画面

- 選択されたプリセットから出題対象のワードを決定する。
- 通常プレイでは、プリセット内のワードからランダムに最大 10 問を出題する。
- 再挑戦プレイでは、前回ミスタイプがあったワードだけを出題する。
- 現在の問題番号、総問題数、対象ワードを表示する。
- 日本語の出題文と、入力対象になるローマ字文字列を両方表示する。
- ユーザーが入力中の状態を、文字単位で視覚的に表示する。
- 入力済みの正しい文字、現在入力すべき文字、未入力の文字、ミスタイプした文字が区別できるようにする。
- 入力中にミスタイプがあったかをリアルタイムに記録する。
- ユーザーが入力した内容をリアルタイムに判定する。
- ミスタイプがあったワードを記録する。
- 全問終了後、結果画面へ進む。

### 2.2.1 入力中の表示仕様

- 出題文として、日本語の文章を大きく表示する。
- 入力対象として、ローマ字文字列を日本語の下に表示する。
- ローマ字文字列を 1 文字ずつ分割して表示する。
- 正しく入力済みの文字は、完了状態として表示する。
- 次に入力する文字は、カーソルまたはハイライトで現在位置が分かるように表示する。
- 未入力の文字は、通常状態として表示する。
- 入力した文字が期待する文字と異なる場合は、ミスタイプ状態として表示する。
- ミスタイプした場合は、現在位置の文字をエラー色にし、短時間の揺れや点滅などでミスが分かるようにする。
- ミスタイプした瞬間に、その問題のワードを「ミスタイプあり」として記録する。
- ミスタイプ後もプレイは継続でき、正しい文字を入力すると次の文字へ進める。
- 1 ワードの全文字を正しく入力し終えたら、自動で次の問題へ進む。
- 入力欄そのものよりも、画面上のワード表示で進捗が分かる体験を優先する。

### 2.2.2 サウンド仕様

- 正しいキー入力時に、短い打鍵音を再生する。
- ミスタイプ時に、正しい入力とは異なるエラー音を再生する。
- 音量はプレイを邪魔しない小さめの音量にする。
- ブラウザの自動再生制限を避けるため、プレイ開始後のユーザー操作をきっかけに音を有効化する。
- 初期実装では外部音声ファイルを使わず、Web Audio API で短い効果音を生成する。
- ユーザーが音をオフにできるミュート切り替えを用意する。

### 2.3 結果画面

- 出題数、正しく入力できた数、ミスタイプがあった数を表示する。
- ミスタイプがあったワードの一覧を表示する。
- ミスタイプが 1 件以上ある場合、再挑戦ボタンを表示する。
- 再挑戦ボタンを押すと、ミスタイプがあったワードだけでプレイ画面へ戻る。
- ミスタイプが 0 件の場合、完了状態として表示し、プリセット一覧へ戻れる。

## 3. プリセットファイル

### 3.1 配置方針

- プリセットは事前にファイルとして用意する。
- JavaScript から読み込みやすい形式にする。
- 初期実装では、静的な JSON ファイルを想定する。

### 3.2 想定ディレクトリ構成

```text
typing_practice/
  index.html
  styles.css
  app.js
  presets/
    basic.json
    programming.json
```

### 3.3 プリセット形式案

```json
{
  "id": "basic",
  "title": "基本ワード",
  "description": "短く入力しやすい基本単語の練習",
  "items": [
    {
      "text": "あるみかんのうえにあるみかん",
      "kana": "あるみかんのうえにあるみかん"
    },
    {
      "text": "おはようございます",
      "kana": "おはようございます"
    }
  ]
}
```

### 3.4 ワードデータ方針

- 画面に表示する日本語は `text` に定義する。
- ローマ字変換の元になる読みは `kana` に定義する。
- 実際に入力判定するローマ字候補は、`kana` からアプリ側で生成する。
- 画面に表示するローマ字は、現在の入力に応じて成立している候補のうち 1 つを表示する。
- プリセット側で特定の入力だけを許可したい場合は、将来的に `readings` で明示できるようにする。

## 4. 出題ルール

- 通常プレイ開始時に、選択したプリセットの `items` からランダムに出題ワードを選ぶ。
- 出題数は最大 10 問とする。
- プリセット内のワードが 10 件未満の場合は、存在する件数だけ出題する。
- 同一プレイ中に同じワードは重複出題しない。
- 再挑戦時は、直前の結果でミスタイプがあったワードのみを出題する。

## 5. ミスタイプ判定

### 5.1 初期実装の判定方針

- 1 キーごとに、入力済みのローマ字列が有効な入力候補の接頭辞になっているかを判定する。
- 判定対象は、日本語の `text` ではなく、`kana` から生成したローマ字入力候補とする。
- 入力済み文字列がどの候補の接頭辞にも一致しない場合、そのキー入力をミスタイプとして記録する。
- ミスタイプ後も、入力位置は進めず、有効なキー入力を待つ。
- 入力済み文字列がいずれかのローマ字入力候補と完全一致した時点で、その問題を完了する。
- 大文字小文字は区別する。
- 入力は小文字のローマ字を基本とする。
- 空白を含む `kana` を扱う場合、空白も入力対象の 1 文字として扱う。

### 5.2 一般的なローマ字入力の許容ルール

- 同じかなに対して一般的に使われる複数のローマ字表記を許可する。
- `じ` は `ji` と `zi` の両方を許可する。
- `ぢ` は `di` と `ji` の両方を許可する。
- `ず` は `zu` と `du` の両方を許可する。
- `づ` は `du` と `zu` の両方を許可する。
- `し` は `shi` と `si` の両方を許可する。
- `ち` は `chi` と `ti` の両方を許可する。
- `つ` は `tsu` と `tu` の両方を許可する。
- `ふ` は `fu` と `hu` の両方を許可する。
- `しゃ`、`しゅ`、`しょ` は `sha` / `sya`、`shu` / `syu`、`sho` / `syo` を許可する。
- `ちゃ`、`ちゅ`、`ちょ` は `cha` / `tya`、`chu` / `tyu`、`cho` / `tyo` を許可する。
- `じゃ`、`じゅ`、`じょ` は `ja` / `jya` / `zya`、`ju` / `jyu` / `zyu`、`jo` / `jyo` / `zyo` を許可する。
- 小さい `っ` は、次の子音の重ね入力を許可する。例: `がっこう` は `gakkou` を許可する。
- 小さい `っ` の単独入力として `xtu` と `ltu` を許可する。
- 小さいかなは、`x` または `l` 始まりの入力を許可する。例: `ぁ` は `xa` と `la` を許可する。
- 撥音 `ん` は、文脈に応じて `n` と `nn` の両方を許可する。
- `ん` の次が母音、`y`、`n` の場合は、曖昧さを避けるため `nn` または `n'` を許可する。
- `かんづめ` のような語では、`kandume` と `kanndume` の両方を許可する。
- 長音 `ー` は `-` を基本入力として扱う。
- 句読点は、プリセットに含める場合のみ入力対象とし、一般的な記号キーで入力する。

### 5.3 ローマ字表示の更新ルール

- プレイ開始時は、代表的なローマ字候補をガイドとして表示する。
- 表示する代表候補は、`si`、`ti`、`tu`、`hu`、`tya`、`sya`、`zya` などの訓令式寄りの表記を優先する。
- ユーザーの入力が複数候補のどれかに絞られた場合、表示中のローマ字ガイドもその候補に合わせて更新する。
- 例: `じ` に対して `j` を入力した場合は `ji` 系の表示に寄せ、`z` を入力した場合は `zi` 系の表示に寄せる。
- 入力済み部分、現在入力位置、未入力部分は、更新後の候補に対して表示する。
- ミスタイプ時は候補を変更せず、現在位置にエラー表示を出す。

### 5.4 将来的に拡張できる判定

- 入力中のミスタイプ回数の記録
- 大文字小文字を区別しないモード
- 日本語かな入力への対応
- バックスペースによる入力位置の戻し
- ミスタイプ時の効果音やアニメーション

## 6. 状態管理

### 6.1 主な状態

- 現在選択中のプリセット
- 現在の出題ワード一覧
- 現在の問題番号
- 現在の文字位置
- 現在の日本語表示文
- 現在のかな読み
- 現在有効なローマ字入力候補
- 現在表示中のローマ字ガイド
- 入力済みローマ字文字列
- 各問題の入力結果
- ミスタイプがあったワード一覧
- 入力中にミスタイプがあったかどうか
- サウンドが有効かどうか
- 通常プレイか再挑戦プレイか

### 6.2 画面遷移

```text
プリセット一覧
  -> プリセット選択
  -> プレイ
  -> 結果
  -> ミスタイプがある場合: 再挑戦
  -> プレイ
  -> 結果
  -> ミスタイプが 0 件の場合: 完了
  -> プリセット一覧
```

## 7. ユーザー操作

- プリセット一覧で練習セットを選択する。
- プレイ画面で表示された日本語を確認し、下に表示されたローマ字をキーボードから直接入力する。
- 1 ワードを最後まで正しく入力すると、自動で次の問題へ進む。
- 必要に応じて、サウンドのオン / オフを切り替える。
- 結果画面で再挑戦、または一覧へ戻る操作を選ぶ。

## 8. 実装範囲

### 8.1 初期実装で対応すること

- HTML / CSS / JavaScript のみで動作するシングルページアプリ
- プリセット JSON の読み込み
- プリセット一覧表示
- ランダム 10 問出題
- 日本語表示文とローマ字入力文字列の同時表示
- 一般的なローマ字表記ゆれを許容する入力判定
- 1 文字ごとのリアルタイム判定
- 入力中の文字状態表示
- 正しい入力時とミスタイプ時の効果音
- サウンドのオン / オフ切り替え
- ミスタイプ単語の結果表示
- ミスタイプ単語だけの再挑戦
- ミスタイプ 0 件になるまで再挑戦可能な流れ

### 8.2 初期実装では対応しないこと

- ユーザーアカウント
- 履歴保存
- スコアの永続化
- 複数人ランキング
- サーバーサイド処理
- 外部ライブラリの導入

## 9. 未決定事項

- プリセット一覧を `presets/index.json` で管理するか、JavaScript 内に定義するか。
- 日本語ワードを初期プリセットに含めるか。
- 入力時間、正確率、WPM などの統計を初期実装に含めるか。
- ミスタイプ時に画面上で一時的に誤入力文字を表示するか、対象文字のエラー状態だけを表示するか。
- ミスタイプ後に Backspace で戻れるようにするか、正しい文字を入力するまで現在位置で止めるか。
- 効果音を Web Audio API で生成するか、音声ファイルとして管理するか。
- ローマ字表記ゆれの候補数が多い場合に、候補生成をどこまで最適化するか。

## 10. 次の実装ステップ案

1. `index.html`、`styles.css`、`app.js` の最小構成を作成する。
2. `presets/` 配下にサンプルプリセット JSON を用意する。
3. プリセット一覧画面を実装する。
4. プレイ画面と回答判定を実装する。
5. 結果画面と再挑戦フローを実装する。
6. ブラウザで基本動作を確認する。
